import React from "react";
import { StyleSheet, Text } from "react-native";

function MyAccountScreen({ navigation }) {
  return <Text>Mina Inställningar</Text>;
}

const styles = StyleSheet.create({});

export default MyAccountScreen;
